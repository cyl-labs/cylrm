import { ScreenStub } from "@/components/screen-stub";

export default function StatsPage() {
  return (
    <ScreenStub
      title="Stats"
      description="Campaign comparison: reply rate, demos per 100 sends, win rate."
      phase="Phase 7"
    />
  );
}
