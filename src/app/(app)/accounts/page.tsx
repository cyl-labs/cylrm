import { ScreenStub } from "@/components/screen-stub";

export default function AccountsPage() {
  return (
    <ScreenStub
      title="Accounts"
      description="Gmail OAuth, daily caps, sends today, bounce rate by domain."
      phase="Phase 2"
    />
  );
}
