import { ScreenStub } from "@/components/screen-stub";

export default function CampaignsPage() {
  return (
    <ScreenStub
      title="Campaigns"
      description="Sequence step editor, enrolled counts, activate/pause."
      phase="Phase 4"
    />
  );
}
