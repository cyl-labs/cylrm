import { ScreenStub } from "@/components/screen-stub";

export default function LeadsPage() {
  return (
    <ScreenStub
      title="Leads"
      description="CSV import, lead lists, duplicate flagging, bulk enroll."
      phase="Phase 1"
    />
  );
}
